% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report-html.R
\name{write_report_html}
\alias{write_report_html}
\title{Write a herald_result as a self-contained HTML report}
\usage{
write_report_html(x, path, title = NULL, ...)
}
\arguments{
\item{x}{A \code{herald_result} object from \code{\link[=validate]{validate()}}.}

\item{path}{Output file path (should end in \code{.html}).}

\item{title}{Document title. Defaults to
\code{"Herald validation <YYYY-MM-DD>"}.}

\item{...}{Ignored.}
}
\value{
\code{path} invisibly.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Renders a \code{herald_result} to a single-file, archival-quality HTML
document. All CSS, JavaScript, and data are embedded inline -- the
file is suitable for submission packages, inspection, and long-term
archiving without external dependencies. The report includes:
\itemize{
\item Header banner with run metadata and finding totals.
\item Per-dataset summary table.
\item Sortable findings table with rule-id deep links to the source
document (CDISC Library / Define-XML / SDTM IG).
\item Applied rules table with per-rule fired and advisory counts.
}
}
\examples{
ae  <- data.frame(STUDYID = "X", USUBJID = "X-001", stringsAsFactors = FALSE)
r   <- validate(files = ae, rules = character(0), quiet = TRUE)
out <- tempfile(fileext = ".html")
on.exit(unlink(out))
write_report_html(r, out)
file.exists(out)

# custom title
write_report_html(r, out, title = "PILOT01 SDTM Conformance Report")

}
\seealso{
\code{\link[=validate]{validate()}} to produce a result, \code{\link[=write_report_xlsx]{write_report_xlsx()}} for
a spreadsheet output, \code{\link[=report]{report()}} to auto-select format from extension.

Other report: 
\code{\link{report}()},
\code{\link{write_report_json}()},
\code{\link{write_report_xlsx}()}
}
\concept{report}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ct-load.R
\name{load_ct}
\alias{load_ct}
\title{Load bundled or cached CDISC Controlled Terminology}
\usage{
load_ct(package = c("sdtm", "adam"), version = "bundled")
}
\arguments{
\item{package}{Character scalar, one of \code{"sdtm"}, \code{"adam"}. Defaults
to \code{"sdtm"}.}

\item{version}{Which release to load. One of:
\itemize{
\item \code{"bundled"} (default) -- the RDS under
\verb{inst/rules/ct/} shipped with the installed herald.
\item \code{"latest-cache"} -- newest entry for \code{package} in the
user CT cache (\code{tools::R_user_dir("herald","cache")}).
\item \code{"YYYY-MM-DD"} -- a specific release already downloaded
into the cache.
\item an absolute \code{.rds} path -- loaded as-is.
}}
}
\value{
Named list with the schema above. Carries attributes
\code{package}, \code{version}, \code{release_date}, \code{source_url}, \code{source_path}.
}
\description{
Returns a named list of codelists -- one entry per CDISC codelist,
keyed by its submission-short-name (e.g. \code{"NY"}, \code{"ACCPARTY"}). Each
entry is a list with \code{codelist_code}, \code{codelist_name}, \code{extensible},
and a \code{terms} data frame holding submission values, NCI concept
ids, and preferred terms.
}
\examples{
\dontrun{
ct <- load_ct("sdtm")
names(ct)[1:5]
ct[["NY"]]$terms
attr(ct, "version")
}

}
\seealso{
\code{\link[=available_ct_releases]{available_ct_releases()}}, \code{\link[=download_ct]{download_ct()}}, \code{\link[=ct_info]{ct_info()}}.

Other ct: 
\code{\link{available_ct_releases}()},
\code{\link{ct_info}()},
\code{\link{download_ct}()}
}
\concept{ct}

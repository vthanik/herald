% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rule-catalog.R
\name{rule_catalog}
\alias{rule_catalog}
\title{Compiled rule catalog}
\usage{
rule_catalog()
}
\value{
A tibble with columns:
\describe{
\item{rule_id}{Rule identifier (e.g. \code{"CG0006"}, \code{"1"},
\code{"define_version_is_2_1"}).}
\item{standard}{CDISC standard family (\code{"SDTM-IG"}, \code{"ADaM-IG"},
\code{"Define-XML"}, \code{"SEND-IG"}, \code{"herald-spec"}).}
\item{authority}{Rule authority (\code{"CDISC"}, \code{"FDA"}, \code{"HERALD"}).}
\item{severity}{Finding severity (\code{"Error"}, \code{"Warning"},
\code{"Medium"}, etc.).}
\item{message}{Short finding message / error code.}
\item{source_document}{Upstream source (e.g. \code{"CDISC ADaM Conformance Rules v5.0"}).}
\item{has_predicate}{\code{TRUE} when an executable check-tree is
compiled for this rule; \code{FALSE} for rules that are authored
as narrative stubs awaiting predicate implementation.}
}
}
\description{
Returns every rule shipped with herald (conformance rules from
\code{inst/rules/rules.rds} plus spec pre-flight rules from
\code{inst/rules/spec_rules.rds}) as a flat tibble.
}
\examples{
cat <- rule_catalog()
nrow(cat)                                       # total rules

# Rules with an executable predicate
sum(cat$has_predicate)

# Filter by standard
cat[cat$standard == "ADaM-IG", ]
cat[cat$standard == "SDTM-IG", ]

# Narrative stubs awaiting implementation
cat[!cat$has_predicate, c("rule_id", "standard", "message")]

# Rules by severity
table(cat$severity)

# Find a rule by ID
cat[cat$rule_id == "CG0006", ]

}
\seealso{
\code{\link[=validate]{validate()}}, \code{\link[=supported_standards]{supported_standards()}}

Other validate: 
\code{\link{supported_standards}()},
\code{\link{validate}()}
}
\concept{validate}

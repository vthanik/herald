% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply-spec.R
\name{apply_spec}
\alias{apply_spec}
\title{Stamp column and dataset attributes from a \code{herald_spec}}
\usage{
apply_spec(datasets, spec)
}
\arguments{
\item{datasets}{Either a single data frame \strong{or} a named list of data
frames. When a single data frame is passed, the dataset name is
inferred from the variable name (\code{dm} -> \code{"DM"}), then
\code{attr(datasets, "dataset_name")}, then \code{"DATA"}. A single data frame
is returned; a list returns a list.}

\item{spec}{A \code{herald_spec} (see \code{\link[=as_herald_spec]{as_herald_spec()}}).}
}
\value{
Same shape as \code{datasets}: a data frame if one was passed, a
named list otherwise.
}
\description{
For each dataset in \code{datasets}, writes \code{attr(., "label")} from
\code{spec$ds_spec} when available, and for each column writes \code{"label"},
\code{"format.sas"}, \code{"sas.length"}, and \code{"xpt_type"} from \code{spec$var_spec}
when a matching row exists. Spec values overwrite any existing
attribute; columns with no spec row are untouched. Datasets absent
from the spec are returned unchanged.

Call this before \code{\link[=validate]{validate()}} when datasets come from CSV, plain
data.frames, or any source that does not itself carry CDISC metadata.
XPT and Dataset-JSON readers already set these attributes at ingest,
so \code{apply_spec()} is optional there.
}
\examples{
dm   <- readRDS(system.file("extdata", "dm.rds", package = "herald"))
adsl <- readRDS(system.file("extdata", "adsl.rds", package = "herald"))
spec <- readRDS(system.file("extdata", "sdtm-spec.rds", package = "herald"))

# ---- Single data frame -- name inferred from variable symbol (dm -> "DM") ----
dm_stamped <- apply_spec(dm, spec)
attr(dm_stamped, "label")           # dataset label from ds_spec
attr(dm_stamped$USUBJID, "label")   # column label from var_spec

# ---- Pipe-friendly: identical result ---------------------------------
dm_stamped2 <- dm |> apply_spec(spec)
attr(dm_stamped2$STUDYID, "label")

# ---- Named list of data frames -- applies spec to each; returns named list ----
datasets <- list(DM = dm)
out <- apply_spec(datasets, spec)
attr(out$DM, "label")
attr(out$DM$USUBJID, "label")

# ---- Explicit dataset_name attribute -- overrides variable-symbol inference ----
raw <- dm
attr(raw, "dataset_name") <- "DM"
stamped <- apply_spec(raw, spec)
attr(stamped, "label")

}
\seealso{
\code{\link[=as_herald_spec]{as_herald_spec()}}, \code{\link[=validate]{validate()}}.

Other spec: 
\code{\link{as_herald_spec}()},
\code{\link{herald_spec}()},
\code{\link{is_herald_spec}()},
\code{\link{read_define_xml}()},
\code{\link{validate_spec}()},
\code{\link{write_define_html}()},
\code{\link{write_define_xml}()}
}
\concept{spec}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict-providers-ext.R
\name{meddra_provider}
\alias{meddra_provider}
\title{MedDRA as a Dictionary Provider}
\usage{
meddra_provider(path, version = "unknown")
}
\arguments{
\item{path}{Directory containing the MedDRA ASCII files OR a direct
path to \code{mdhier.asc}. When a directory is given, the factory
looks for \code{mdhier.asc} (required) and \code{llt.asc} (optional).}

\item{version}{MedDRA release tag (e.g. \code{"27.0"}). User-supplied;
MedDRA files don't self-identify. Defaults to \code{"unknown"}.}
}
\value{
A \code{herald_dict_provider} with name \code{"meddra"}.
}
\description{
Reads an MSSO MedDRA ASCII distribution from \code{path} and returns a
\code{herald_dict_provider}. Supports membership checks at any level:
\code{"llt"}, \code{"pt"}, \code{"hlt"}, \code{"hlgt"}, \code{"soc"}. Since MedDRA is
licensed, herald never bundles the data -- the user must supply a
valid local distribution.
}
\examples{
# Requires a licensed MedDRA ASCII distribution (user-supplied)
if (interactive()) {
  p <- meddra_provider("/path/to/meddra_27_0/MedAscii", version = "27.0")

  # Preferred-term lookup
  p$contains("Headache", field = "pt")          # TRUE
  p$contains("HEADACHE", field = "pt")          # FALSE (case-sensitive)
  p$contains("HEADACHE", field = "pt",
             ignore_case = TRUE)                 # TRUE

  # High-level term and SOC lookup
  p$contains("Nervous system disorders", field = "soc")
  p$contains("Headaches NEC", field = "hlt")

  # Lowest-level term (when llt.asc is present)
  p$contains("Tension headache", field = "llt")

  # Inspect provider metadata
  p$info()$version          # "27.0"
  p$info()$fields           # available query fields

  # Register globally for all subsequent validate() calls
  register_dictionary("meddra", p)
}

}
\seealso{
\code{\link[=register_dictionary]{register_dictionary()}}, \code{\link[=whodrug_provider]{whodrug_provider()}}.

Other dict: 
\code{\link{ct_provider}()},
\code{\link{custom_provider}()},
\code{\link{list_dictionaries}()},
\code{\link{loinc_provider}()},
\code{\link{new_dict_provider}()},
\code{\link{register_dictionary}()},
\code{\link{snomed_provider}()},
\code{\link{srs_provider}()},
\code{\link{unregister_dictionary}()},
\code{\link{whodrug_provider}()}
}
\concept{dict}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict-providers-ct.R
\name{ct_provider}
\alias{ct_provider}
\title{CDISC Controlled Terminology as a Dictionary Provider}
\usage{
ct_provider(package = c("sdtm", "adam"), version = "bundled")
}
\arguments{
\item{package}{One of \code{"sdtm"}, \code{"adam"}. Defaults to \code{"sdtm"}.}

\item{version}{Same semantics as \code{\link[=load_ct]{load_ct()}}:
\code{"bundled"} | \code{"latest-cache"} | \code{"YYYY-MM-DD"} | absolute
\code{.rds} path.}
}
\value{
A \code{herald_dict_provider} with provider name
\code{paste0("ct-", package)}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Returns a \code{herald_dict_provider} that serves the bundled (or
user-cached) CDISC CT. Rule ops that need to check codelist
membership look up this provider under the name \code{"ct-sdtm"} or
\code{"ct-adam"}.
}
\section{Provider protocol}{
\code{contains(value, field, ignore_case)} interprets \code{field} as the
codelist short name (e.g. \code{"NY"}, \code{"ARMNULRS"}), the NCI C-code, or
the long human-readable codelist name -- the same dispatch order as
\code{.lookup_codelist()} used by SDTM ops. Returns \code{NA} when \code{field} is
missing or unknown so missing-codelist conditions surface as
advisory findings, not errors.
}

\section{Caching}{
The bundled CT is loaded once via \code{\link[=load_ct]{load_ct()}} and held in the
provider closure for the lifetime of the object. Cache hits from a
prior \code{download_ct()} are addressable through \verb{version =}.
}

\examples{
# Bundled SDTM CT (always available, no network)
p <- ct_provider("sdtm")
p$contains("Y", field = "NY")           # TRUE
p$contains("UNKNOWN", field = "NY")     # FALSE
p$contains(c("Y", "N", "BAD"), field = "NY")  # TRUE TRUE FALSE
p$info()$version
p$info()$codelist_count

# Case-insensitive lookup
p$contains("y", field = "NY", ignore_case = TRUE)  # TRUE

# ADaM CT provider
pa <- ct_provider("adam")
pa$info()$size_rows

# Register globally so every validate() picks it up
register_dictionary("ct-sdtm", p)
list_dictionaries()
unregister_dictionary("ct-sdtm")

# Pinned version from user cache (requires prior download_ct())
if (interactive()) {
  p_pinned <- ct_provider("sdtm", version = "2024-09-27")
  p_pinned$info()$version
}

}
\seealso{
\code{\link[=load_ct]{load_ct()}}, \code{\link[=register_dictionary]{register_dictionary()}}.

Other dict: 
\code{\link{custom_provider}()},
\code{\link{list_dictionaries}()},
\code{\link{loinc_provider}()},
\code{\link{meddra_provider}()},
\code{\link{new_dict_provider}()},
\code{\link{register_dictionary}()},
\code{\link{snomed_provider}()},
\code{\link{srs_provider}()},
\code{\link{unregister_dictionary}()},
\code{\link{whodrug_provider}()}
}
\concept{dict}

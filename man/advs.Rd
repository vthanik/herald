% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{advs}
\alias{advs}
\title{Pilot ADVS (Vital Signs Analysis Dataset, BDS)}
\format{
A data frame with \strong{6,138 rows} and \strong{35 columns}.
\describe{
\item{\code{STUDYID}}{\emph{Study Identifier} (text, len 12).}
\item{\code{SITEID}}{\emph{Study Site Identifier} (text, len 3).}
\item{\code{USUBJID}}{\emph{Unique Subject Identifier} (text, len 11).}
\item{\code{AGE}}{\emph{Age} (integer, len 8).}
\item{\code{AGEGR1}}{\emph{Pooled Age Group 1} (text, len 5).}
\item{\code{AGEGR1N}}{\emph{Pooled Age Group 1 (N)} (integer, len 8).}
\item{\code{RACE}}{\emph{Race} (text, len 32).}
\item{\code{RACEN}}{\emph{Race (N)} (integer, len 8).}
\item{\code{SEX}}{\emph{Sex} (text, len 1).}
\item{\code{SAFFL}}{\emph{Safety Population Flag} (text, len 1).}
\item{\code{TRTSDT}}{\emph{Date of First Exposure to Treatment} (numeric date,
len 8).}
\item{\code{TRTEDT}}{\emph{Date of Last Exposure to Treatment} (numeric date,
len 8).}
\item{\code{TRTP}}{\emph{Planned Treatment} (text, len 20).}
\item{\code{TRTPN}}{\emph{Planned Treatment (N)} (integer, len 8).}
\item{\code{TRTA}}{\emph{Actual Treatment} (text, len 20).}
\item{\code{TRTAN}}{\emph{Actual Treatment (N)} (integer, len 8).}
\item{\code{PARAMCD}}{\emph{Parameter Code} (text, len 8) -- short code (e.g.
\code{"DIABP"}, \code{"SYSBP"}, \code{"PULSE"}, \code{"TEMP"}, \code{"WEIGHT"}).}
\item{\code{PARAM}}{\emph{Parameter} (text, len 100) -- human-readable name
(e.g. \code{"Diastolic Blood Pressure (mmHg)"}).}
\item{\code{PARAMN}}{\emph{Parameter (N)} (integer, len 8) -- numeric companion
to \code{PARAMCD}.}
\item{\code{ADT}}{\emph{Analysis Date} (numeric date, len 8).}
\item{\code{ADY}}{\emph{Analysis Relative Day} (integer, len 8) -- study day
of \code{ADT} relative to \code{TRTSDT}.}
\item{\code{ATPTN}}{\emph{Analysis Timepoint (N)} (integer, len 8).}
\item{\code{ATPT}}{\emph{Analysis Timepoint} (text, len 30) -- e.g. \code{"PRE-DOSE"}.}
\item{\code{AVISIT}}{\emph{Analysis Visit} (text, len 16) -- normalised visit
name (\code{"BASELINE"}, \code{"WEEK 8"}, etc.).}
\item{\code{AVISITN}}{\emph{Analysis Visit (N)} (integer, len 8).}
\item{\code{AVAL}}{\emph{Analysis Value} (float, len 8) -- the value used in
analyses.}
\item{\code{BASE}}{\emph{Baseline Value} (float, len 8).}
\item{\code{BASETYPE}}{\emph{Baseline Type} (text, len 30) -- baseline
definition flag for analyses with multiple baselines.}
\item{\code{CHG}}{\emph{Change from Baseline} (float, len 8) -- \code{AVAL - BASE}.}
\item{\code{PCHG}}{\emph{Percent Change from Baseline} (float, len 8) --
\code{100 * CHG / BASE}.}
\item{\code{VISITNUM}}{\emph{Visit Number} (float, len 8) -- numeric visit
order from SDTM.}
\item{\code{VISIT}}{\emph{Visit Name} (text, len 19) -- visit label from SDTM.}
\item{\code{VSSEQ}}{\emph{Sequence Number} (integer, len 8) -- record sequence
within (\code{USUBJID}, \code{VSTESTCD}).}
\item{\code{ANL01FL}}{\emph{Analysis Flag 01} (text, len 1) -- primary-analysis
record flag.}
\item{\code{ABLFL}}{\emph{Baseline Record Flag} (text, len 1) -- \code{"Y"} on the
row used to derive \code{BASE}.}
}
}
\source{
CDISC SDTM/ADaM Pilot Submission Package (public domain).
6,138 vital-sign assessments across the 50 pilot subjects.
}
\usage{
advs
}
\description{
An ADVS dataset from the CDISC ADaM pilot submission with 6,138 records
across all scheduled vital-sign assessments for the 50 pilot subjects.
ADVS follows the ADaM Basic Data Structure (BDS): one row per
(subject, parameter, visit, timepoint), with \code{AVAL} carrying the
analysis value, \code{BASE} the baseline, and \code{CHG}/\code{PCHG} the absolute /
percent change from baseline. The largest of the pilot datasets --
useful for performance benchmarking and BDS-rule coverage testing.

All 35 variables shipped here are documented below using labels from
the bundled \code{\link{adam_spec}}.
}
\examples{
nrow(advs)
unique(advs$PARAMCD)

# Records per parameter
table(advs$PARAMCD)

# Baseline records only
bl <- advs[advs$ABLFL == "Y", ]
nrow(bl)

# Mean change from baseline by parameter and visit (analysis subset)
anl <- advs[advs$ANL01FL == "Y" & !is.na(advs$CHG), ]
aggregate(CHG ~ PARAMCD + AVISIT, data = anl, mean)
}
\seealso{
\link{adsl} (population denominator); \link{adam_spec}.

Other pilot-data: 
\code{\link{adae}},
\code{\link{adam_spec}},
\code{\link{adsl}},
\code{\link{dm}},
\code{\link{pilot-data}},
\code{\link{sdtm_spec}}
}
\concept{pilot-data}
\keyword{datasets}

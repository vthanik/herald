% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parquet-write.R
\name{write_parquet}
\alias{write_parquet}
\title{Write a data frame to Apache Parquet with CDISC column attributes}
\usage{
write_parquet(x, file, dataset = NULL, label = NULL)
}
\arguments{
\item{x}{A data frame. Column attributes (\code{label}, \code{format.sas},
\code{sas.length}, \code{xpt_type}) are written to Parquet key/value
metadata when present.}

\item{file}{Output path (should end in \code{.parquet}).}

\item{dataset}{Dataset name (e.g., \code{"DM"}). Default: inferred from
the variable name (\code{dm} -> \code{"DM"}), then
\code{attr(x, "dataset_name")}, then the uppercase file stem.}

\item{label}{Dataset label. Default: \code{attr(x, "label")}.}
}
\value{
\code{x} invisibly.
}
\description{
Writes a data frame to a Parquet file, embedding CDISC metadata
(column labels, SAS formats, lengths, and XPT types) in the file's
key/value metadata under \verb{herald.*} keys. This ensures a full
round-trip: \code{read_parquet()} restores every attribute that
\code{write_parquet()} saved.

Requires the \code{arrow} package.
}
\examples{
\dontshow{if (requireNamespace("arrow", quietly = TRUE)) withAutoprint(\{ # examplesIf}
dm   <- readRDS(system.file("extdata", "dm.rds", package = "herald"))
spec <- readRDS(system.file("extdata", "sdtm-spec.rds", package = "herald"))
dm   <- apply_spec(dm, spec)
out  <- tempfile(fileext = ".parquet")
on.exit(unlink(out))

# ---- Dataset name inferred from variable symbol (dm -> "DM"), with label ----
write_parquet(dm, out, label = "Demographics")
attr(read_parquet(out), "dataset_name")   # "DM"

# ---- Explicit dataset and label overrides ----------------------------
out2 <- tempfile(fileext = ".parquet")
on.exit(unlink(out2), add = TRUE)
write_parquet(dm, out2, dataset = "DM", label = "Demographics SDTM")
attr(read_parquet(out2), "label")

# ---- Plain data frame (no apply_spec) -- name from variable symbol ----
ae <- data.frame(STUDYID = "X", USUBJID = "X-001", stringsAsFactors = FALSE)
out3 <- tempfile(fileext = ".parquet")
on.exit(unlink(out3), add = TRUE)
write_parquet(ae, out3, dataset = "AE", label = "Adverse Events")

# ---- Round-trip: write and read back, compare column counts ----------
write_parquet(dm, out)
dm_rt <- read_parquet(out)
identical(names(dm), names(dm_rt))
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=read_parquet]{read_parquet()}}, \code{\link[=write_xpt]{write_xpt()}}, \code{\link[=write_json]{write_json()}}.

Other io: 
\code{\link{convert_dataset}()},
\code{\link{read_json}()},
\code{\link{read_parquet}()},
\code{\link{read_xpt}()},
\code{\link{write_json}()},
\code{\link{write_xpt}()}
}
\concept{io}

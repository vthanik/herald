% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec-validate.R
\name{validate_spec}
\alias{validate_spec}
\title{Validate a herald_spec for Define-XML completeness}
\usage{
validate_spec(spec, report = NULL, view = TRUE)
}
\arguments{
\item{spec}{A \code{herald_spec} object from \code{\link[=as_herald_spec]{as_herald_spec()}} or
\code{\link[=herald_spec]{herald_spec()}}.}

\item{report}{File path for the HTML report.  Defaults to a temporary file.
Ignored when there are no issues.}

\item{view}{Logical.  If \code{TRUE} (default) and issues are found, the report
is opened in the IDE viewer pane before aborting.}
}
\value{
Invisibly, when the spec is clean.  Otherwise an error of class
\code{herald_error_validation} is raised.
}
\description{
Runs the built-in spec-validation rules (standard: \code{herald-spec}) against
the slots of \code{spec}.  If any issues are found a detailed HTML report is
written to \code{report}, opened in the IDE viewer, and an error is raised.
}
\examples{
# ---- Valid spec -- returns invisibly (no issues found) ---------------
spec_ok <- as_herald_spec(
  ds_spec = data.frame(
    dataset = "DM",
    label   = "Demographics",
    stringsAsFactors = FALSE
  )
)
invisible(validate_spec(spec_ok))   # returns NULL invisibly

# ---- Suppress the viewer for automated pipelines ---------------------
invisible(validate_spec(spec_ok, view = FALSE))

# ---- Send the HTML report to a known path (not a tempfile) -----------
report_path <- tempfile(fileext = ".html")
on.exit(unlink(report_path))
invisible(validate_spec(spec_ok, report = report_path, view = FALSE))
file.exists(report_path)   # FALSE -- file is only written when issues exist

# ---- Invalid spec triggers an error (catch it for demonstration) -----
spec_bad <- as_herald_spec(
  ds_spec = data.frame(dataset = "DM", stringsAsFactors = FALSE)
)
tryCatch(
  validate_spec(spec_bad, view = FALSE),
  herald_error_validation = function(e) conditionMessage(e)
)

}
\seealso{
Other spec: 
\code{\link{apply_spec}()},
\code{\link{as_herald_spec}()},
\code{\link{herald_spec}()},
\code{\link{is_herald_spec}()},
\code{\link{read_define_xml}()},
\code{\link{write_define_html}()},
\code{\link{write_define_xml}()}
}
\concept{spec}

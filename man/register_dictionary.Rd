% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict-provider.R
\name{register_dictionary}
\alias{register_dictionary}
\title{Install a dictionary provider in the session registry}
\usage{
register_dictionary(name, provider)
}
\arguments{
\item{name}{Character scalar -- canonical short name (e.g. \code{"meddra"},
\code{"whodrug"}, \code{"srs"}, \code{"sponsor-race"}).}

\item{provider}{A \code{herald_dict_provider} (from one of the factories:
\code{\link[=ct_provider]{ct_provider()}}, \code{\link[=srs_provider]{srs_provider()}}, \code{\link[=meddra_provider]{meddra_provider()}}, ...).}
}
\value{
\code{invisible(provider)}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Adds \code{provider} to the session-level dictionary registry under
\code{name}. After this call every subsequent \code{\link[=validate]{validate()}} picks up the
provider automatically unless explicitly overridden by the
\verb{dictionaries =} argument:
\itemize{
\item Sponsors install a MedDRA / WHO-Drug provider once at the top of
a pipeline; downstream \code{validate()} calls inherit it.
\item The same registry powers \code{\link[=list_dictionaries]{list_dictionaries()}} for reporting.
\item Use \code{\link[=unregister_dictionary]{unregister_dictionary()}} to remove an entry.
}
}
\examples{
# Register bundled SDTM CT and inspect the registry
p <- ct_provider("sdtm")
register_dictionary("sdtm", p)
list_dictionaries()

# Register a custom sponsor-private dictionary
codes <- data.frame(site = c("S01", "S02", "S03"), stringsAsFactors = FALSE)
sponsor_p <- custom_provider(codes, name = "site-codes", fields = "site")
register_dictionary("site-codes", sponsor_p)
list_dictionaries()

# Clean up
unregister_dictionary("sdtm")
unregister_dictionary("site-codes")

}
\seealso{
Other dict: 
\code{\link{ct_provider}()},
\code{\link{custom_provider}()},
\code{\link{list_dictionaries}()},
\code{\link{loinc_provider}()},
\code{\link{meddra_provider}()},
\code{\link{new_dict_provider}()},
\code{\link{snomed_provider}()},
\code{\link{srs_provider}()},
\code{\link{unregister_dictionary}()},
\code{\link{whodrug_provider}()}
}
\concept{dict}

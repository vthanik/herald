% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/json-write.R
\name{write_json}
\alias{write_json}
\title{Write a data frame as CDISC Dataset-JSON v1.1}
\usage{
write_json(
  x,
  file,
  dataset = NULL,
  label = NULL,
  study_oid = "",
  metadata_version_oid = "",
  metadata_ref = NULL,
  originator = "herald"
)
}
\arguments{
\item{x}{A data frame.}

\item{file}{Output file path (should end in \code{.json}).}

\item{dataset}{Dataset name (e.g., \code{"DM"}). Default: inferred from
the \code{"dataset_name"} attribute or the file name.}

\item{label}{Dataset label. Default: from the \code{"label"} attribute.}

\item{study_oid}{Study OID for metadata. Default: \code{""}.}

\item{metadata_version_oid}{Metadata version OID. Default: \code{""}.}

\item{metadata_ref}{Path to define.xml. Default: \code{NULL}.}

\item{originator}{Originator name. Default: \code{"herald"}.}
}
\value{
\code{x} invisibly (the input data frame, not the file path).
}
\description{
Writes a data frame to a CDISC Dataset-JSON v1.1 file following the
official CDISC specification. Column labels, types, and lengths are
extracted from attributes on each column. If the
\code{herald.sort_keys} attribute is set, the data is sorted before
writing. JSON is always UTF-8.
}
\examples{
dm   <- readRDS(system.file("extdata", "dm.rds", package = "herald"))
spec <- readRDS(system.file("extdata", "sdtm-spec.rds", package = "herald"))
dm   <- apply_spec(dm, spec)
file <- tempfile(fileext = ".json")
on.exit(unlink(file))
write_json(dm, file, label = "Demographics")

}
\seealso{
\code{\link[=read_json]{read_json()}} for reading, \code{\link[=write_xpt]{write_xpt()}} for XPT I/O.

Other io: 
\code{\link{convert_dataset}()},
\code{\link{read_json}()},
\code{\link{read_parquet}()},
\code{\link{read_xpt}()},
\code{\link{write_parquet}()},
\code{\link{write_xpt}()}
}
\concept{io}

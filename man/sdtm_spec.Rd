% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{sdtm_spec}
\alias{sdtm_spec}
\title{Pilot SDTM specification (herald_spec)}
\format{
A \code{herald_spec} (S3 list, length 2) with elements \code{ds_spec}
(31 rows, 2 columns) and \code{var_spec} (747 rows, 10 columns).
}
\source{
CDISC SDTM/ADaM Pilot Submission Package (public domain),
loaded with \code{\link[=read_define_xml]{read_define_xml()}}.
}
\usage{
sdtm_spec
}
\description{
A \code{herald_spec} object covering \strong{31 SDTM datasets} and \strong{747 variable
definitions} from the CDISC pilot study, derived from the pilot
\code{define.xml} via \code{\link[=read_define_xml]{read_define_xml()}} + \code{\link[=as_herald_spec]{as_herald_spec()}}. Pass to
\code{\link[=apply_spec]{apply_spec()}} before validating SDTM datasets to activate
label-, length-, and codelist-dependent rules.
}
\details{
\subsection{Object structure}{

A \code{herald_spec} is a length-2 list with class \code{c("herald_spec", "list")}:
\itemize{
\item \verb{$ds_spec} -- 31 rows x 2 columns (\code{dataset}, \code{label}).
\item \verb{$var_spec} -- 747 rows x 10 columns. Every variable across every
dataset.
}
}

\subsection{Datasets covered (\code{ds_spec})}{

All 31 SDTM datasets from the pilot:\tabular{ll}{
   Group \tab Datasets \cr
   Trial design \tab \code{TA}, \code{TE}, \code{TI}, \code{TS}, \code{TV} \cr
   Demographics / disposition \tab \code{DM}, \code{DS}, \code{MH}, \code{SC}, \code{SE}, \code{SV} \cr
   Interventions \tab \code{CM}, \code{EX} \cr
   Events \tab \code{AE} \cr
   Findings \tab \code{LBCH}, \code{LBHE}, \code{LBUR} (split lab), \code{VS}, \code{QSCO}, \code{QSDA}, \code{QSGI}, \code{QSHI}, \code{QSMM}, \code{QSNI} (split questionnaires) \cr
   Special-purpose \tab \code{RELREC} \cr
   Supplemental qualifiers \tab \code{SUPPAE}, \code{SUPPDM}, \code{SUPPDS}, \code{SUPPLBCH}, \code{SUPPLBHE}, \code{SUPPLBUR} \cr
}

}

\subsection{Variable metadata columns (\code{var_spec})}{

All 10 columns shipped on \code{var_spec}:

\describe{
\item{\code{dataset}}{Dataset name the variable belongs to (e.g. \code{"DM"}).}
\item{\code{variable}}{Variable name (e.g. \code{"USUBJID"}).}
\item{\code{label}}{CDISC SDTM variable label (e.g. \code{"Unique Subject Identifier"}).}
\item{\code{data_type}}{One of \code{"text"}, \code{"integer"}, \code{"float"}, \code{"date"},
\code{"datetime"}, \code{"time"}, \code{"partial_date"}.}
\item{\code{length}}{Storage length as a string (numeric for \code{text},
\code{"8"} for numerics).}
\item{\code{origin}}{Variable origin -- \code{"CRF"}, \code{"Assigned"}, \code{"Derived"},
\code{"Protocol"}, \code{"eDT"}, etc.}
\item{\code{codelist}}{Codelist OID when the variable is controlled
(e.g. \code{"CL.SEX"}, \code{"CL.AGEU"}); empty string otherwise.}
\item{\code{mandatory}}{\code{"Yes"} / \code{"No"} -- whether the variable is
required by SDTM-IG.}
\item{\code{order}}{Variable order as a string -- position within the
dataset.}
\item{\code{format}}{Display format (e.g. \code{"$8."}, \code{"DATE9."}); empty
string when unset.}
}
}
}
\examples{
is_herald_spec(sdtm_spec)

# Datasets covered
sdtm_spec$ds_spec

# Total variable definitions
nrow(sdtm_spec$var_spec)

# All metadata columns shipped on var_spec
names(sdtm_spec$var_spec)

# Variables in DM, with their controlled-terminology codelists
dm_vars <- sdtm_spec$var_spec[sdtm_spec$var_spec$dataset == "DM", ]
dm_vars[, c("variable", "label", "codelist", "mandatory")]

# Mandatory variable count per dataset
mand <- sdtm_spec$var_spec[sdtm_spec$var_spec$mandatory == "Yes", ]
table(mand$dataset)

\donttest{
# Stamp DM with spec attributes, then validate
dm_stamped <- apply_spec(dm, sdtm_spec)
attr(dm_stamped$USUBJID, "label")

r <- validate(files = dm_stamped, quiet = TRUE)
}
}
\seealso{
\code{\link[=as_herald_spec]{as_herald_spec()}}, \code{\link[=apply_spec]{apply_spec()}}, \code{\link[=validate]{validate()}}, \link{adam_spec}.

Other pilot-data: 
\code{\link{adae}},
\code{\link{adam_spec}},
\code{\link{adsl}},
\code{\link{advs}},
\code{\link{dm}},
\code{\link{pilot-data}}
}
\concept{pilot-data}
\keyword{datasets}

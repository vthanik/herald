% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict-providers-ext.R
\name{whodrug_provider}
\alias{whodrug_provider}
\title{WHO-Drug as a Dictionary Provider}
\usage{
whodrug_provider(path, version = "unknown", format = "b3")
}
\arguments{
\item{path}{Directory containing the B3 distribution. Looks for
\code{DD.txt} (required) and \code{DDA.txt} (optional alternate names).}

\item{version}{WHO-Drug release tag (e.g. \code{"2026-Mar-01"}).
User-supplied.}

\item{format}{Currently only \code{"b3"} is supported; placeholder for
future C3 support.}
}
\value{
A \code{herald_dict_provider} with name \code{"whodrug"}.
}
\description{
Reads a UMC WHO-Drug B3 distribution from \code{path} and returns a
\code{herald_dict_provider} serving drug-name membership checks. Since
WHO-Drug is licensed, herald never bundles the data.
}
\examples{
# Requires a licensed WHO-Drug B3 distribution (user-supplied)
if (interactive()) {
  p <- whodrug_provider("/path/to/whodrug-b3", version = "2026-Mar-01")

  # Drug-name lookup
  p$contains("ASPIRIN", field = "drug_name")       # TRUE
  p$contains("aspirin", field = "drug_name",
             ignore_case = TRUE)                    # TRUE

  # Alternate names (requires DDA.txt in the distribution)
  p$contains("ACETYLSALICYLIC ACID", field = "alternate_name")

  # Drug record number lookup
  p$contains("000062", field = "drug_record_number")

  # Provider metadata
  p$info()$version          # "2026-Mar-01"
  p$info()$size_rows

  # Register globally
  register_dictionary("whodrug", p)
}

}
\seealso{
\code{\link[=register_dictionary]{register_dictionary()}}, \code{\link[=meddra_provider]{meddra_provider()}}.

Other dict: 
\code{\link{ct_provider}()},
\code{\link{custom_provider}()},
\code{\link{list_dictionaries}()},
\code{\link{loinc_provider}()},
\code{\link{meddra_provider}()},
\code{\link{new_dict_provider}()},
\code{\link{register_dictionary}()},
\code{\link{snomed_provider}()},
\code{\link{srs_provider}()},
\code{\link{unregister_dictionary}()}
}
\concept{dict}

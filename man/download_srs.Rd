% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dict-providers-srs.R
\name{download_srs}
\alias{download_srs}
\title{Download + cache the FDA SRS / UNII table}
\usage{
download_srs(
  version = format(Sys.Date(), "\%Y-\%m-\%d"),
  dest = .ct_cache_dir(),
  force = FALSE,
  timeout = 180L,
  quiet = FALSE
)
}
\arguments{
\item{version}{Release tag for the cache path. Defaults to today's
ISO date (\code{format(Sys.Date(), "\%Y-\%m-\%d")}), since the FDA does
not expose a version in the filename.}

\item{dest}{Target directory. Defaults to
\code{tools::R_user_dir("herald", "cache")}.}

\item{force}{Re-download even when the RDS already exists.}

\item{timeout}{Seconds for \code{utils::download.file()}. Default 180.}

\item{quiet}{Suppress progress output.}
}
\value{
The path to the generated RDS, invisibly.
}
\description{
Fetches the FDA's public bulk download, parses the tab-delimited
body inside the ZIP into a tidy tibble, writes it to the user
cache as an RDS, and updates the cache manifest. Idempotent --
re-runs short-circuit when the file is already cached.
}
\examples{
# Download the FDA SRS table (requires internet)
if (interactive()) {
  # Default: cache under tools::R_user_dir("herald", "cache")
  download_srs()

  # Download to a specific directory
  download_srs(dest = tempdir())

  # Tag with an explicit version (useful for reproducibility)
  download_srs(version = "2026-04-01", dest = tempdir())

  # Re-download even when cached
  download_srs(force = TRUE, quiet = TRUE)
}

}
\seealso{
\code{\link[=srs_provider]{srs_provider()}}, \code{\link[=download_ct]{download_ct()}}.

Other ct: 
\code{\link{available_ct_releases}()},
\code{\link{ct_info}()},
\code{\link{download_ct}()},
\code{\link{load_ct}()}
}
\concept{ct}
